// ====================================
// أوامر الألعاب
// ====================================

function handleGamesCommands(msg, chat, command) {
    
    if (command === 'اخر_حرف') {
        const words = ['تفاح', 'موز', 'برتقال', 'عنب', 'فراولة', 'بطيخ', 'مانجو', 'أناناس', 'كرز', 'خوخ'];
        const randomWord = words[Math.floor(Math.random() * words.length)];
        const lastChar = randomWord[randomWord.length - 1];
        msg.reply(`🔤 *الكلمة:* ${randomWord}\n🔄 *آخر حرف:* ${lastChar}\n⏳ لديك 30 ثانية`);
    }

    if (command === 'سؤال') {
        const questions = [
            'ما عاصمة فرنسا؟',
            'كم شهر في السنة؟',
            'ما أكبر محيط في العالم؟',
            'من أول من صعد للقمر؟',
            'ما أكبر قارة في العالم؟',
            'كم عدد ألوان قوس قزح؟'
        ];
        msg.reply(`❓ *سؤال:* ${questions[Math.floor(Math.random() * questions.length)]}`);
    }

    if (command === 'لوخيروك') {
        const choices = [
            'تسافر للمريخ ولا تعيش تحت البحر؟',
            'تكون غني وما عندك أصدقاء ولا فقير وعندك أصحاب أوفياء؟',
            'تقدر تقرأ الأفكار ولا تقدر تطير؟',
            'تعيش 100 سنة بصحة ولا 50 سنة بثراء؟',
            'تكون مشهور وما تعرف تستمتع بحياتك ولا تكون عادي وتعيش سعيد؟'
        ];
        msg.reply(`🤔 *لو خيروك:* ${choices[Math.floor(Math.random() * choices.length)]}`);
    }
}

module.exports = { handleGamesCommands };